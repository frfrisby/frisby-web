<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>software.frisby.web</groupId>
        <artifactId>frisby-web</artifactId>
        <version>1.4.1-SNAPSHOT</version>
    </parent>

    <artifactId>bom</artifactId>
    <packaging>pom</packaging>
    <name>${project.artifactId}</name>
    <url>https://github.com/frfrisby/frisby-web</url>

    <dependencyManagement>
        <dependencies>
            <!-- Pin the frisby-core version this library was built and tested against.
                 Consumers who need a newer frisby-core release can override this by
                 importing their preferred frisby-core:bom before this BOM.
                 Literal version required: flatten-maven-plugin does not interpolate
                 ${...} expressions in <dependencyManagement> version elements when
                 flattening a BOM, so property references would remain unresolved in
                 the published artifact.  BomVersionSyncTest enforces that this value
                 stays in sync with frisby-core.version in the root pom.xml. -->
            <dependency>
                <groupId>software.frisby.core</groupId>
                <artifactId>bom</artifactId>
                <version>1.3.1</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Jetty BOM — imported BEFORE jersey-bom so that our pinned Jetty version
                 takes precedence over whatever Jetty version Jersey's BOM declares.
                 Jersey 3.x requires Jetty 12; mixing versions causes runtime failures.
                 Literal version required — see note above.
                 BomVersionSyncTest enforces sync with jetty.version in the root pom.xml. -->
            <dependency>
                <groupId>org.eclipse.jetty</groupId>
                <artifactId>jetty-bom</artifactId>
                <version>12.0.37</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Pin the Jersey version this library was built and tested against.
                 Literal version required — see note above.
                 BomVersionSyncTest enforces sync with jersey.version in the root pom.xml. -->
            <dependency>
                <groupId>org.glassfish.jersey</groupId>
                <artifactId>jersey-bom</artifactId>
                <version>3.1.12</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>serial</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>client</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>client-sse</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>basic-security</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>oauth2-security</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>server</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>server-basic-security</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>server-oauth2-security</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>software.frisby.web</groupId>
                <artifactId>jackson-serializer</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-install-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <configuration>
                    <flattenMode>bom</flattenMode>
                    <pomElements>
                        <distributionManagement>remove</distributionManagement>
                        <properties>remove</properties>
                    </pomElements>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
